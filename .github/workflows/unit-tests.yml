name: unit-tests
on: [pull_request]

jobs:
  test:
    name: Unit tests on Node 20
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Dummy test
        run: echo "GitHub Actions works!"
          